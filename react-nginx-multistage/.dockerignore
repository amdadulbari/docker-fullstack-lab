# Keep the build context small and reproducible. node_modules is rebuilt inside
# the builder stage, and dist is regenerated by `npm run build`.
node_modules
dist
.git
.gitignore
Dockerfile
.dockerignore
docker-compose.yml
npm-debug.log
*.log
.DS_Store
.env
