# =============================================================================
# .env.example — Copy this file to .env and fill in real values before running.
# Never commit the actual .env file to version control.
# =============================================================================

# ---------------------------------------------------------------------------
# Backend (Node.js / Express)
# ---------------------------------------------------------------------------

# Port the Express server listens on (used inside the Docker container).
PORT=4000

# PostgreSQL connection details.
# When running with Docker Compose, DB_HOST must match the service name in
# docker-compose.yml (i.e. "db").  For local dev without Docker use "localhost".
DB_HOST=db
DB_PORT=5432
DB_NAME=tododb
DB_USER=todouser
DB_PASSWORD=todopassword

# Allowed CORS origin.  Set to the URL of the frontend in production.
# Use * only for development/testing.
FRONTEND_URL=http://localhost:3000

# ---------------------------------------------------------------------------
# Frontend (Vite build-time variables)
# Variables prefixed with VITE_ are exposed to the browser bundle.
# ---------------------------------------------------------------------------

# Base URL for the Axios API client.
# '/api/v1' works when nginx (or Vite's dev proxy) forwards /api to the backend.
# For a standalone backend without a proxy set the full URL, e.g.:
#   VITE_API_URL=http://localhost:4000/api/v1
VITE_API_URL=/api/v1
