{
  "name": "expense-tracker",
  "version": "1.0.0",
  "description": "An expense tracker built with React + Vite — used to demonstrate a multi-stage production Dockerfile (Node build -> Nginx serve).",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 0.0.0.0",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.1",
    "vite": "^5.1.4"
  }
}
