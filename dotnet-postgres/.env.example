# ════════════════════════════════════════════════════════════════
# .env.example — ASP.NET Core 8 + PostgreSQL
#
# Copy this file to .env:
#   cp .env.example .env
#
# NEVER commit .env to version control.
# ════════════════════════════════════════════════════════════════

# ── PostgreSQL ───────────────────────────────────────────────────
DB_HOST=db          # 'db' for Docker Compose (service name); 'localhost' for local dev
DB_PORT=5432
DB_NAME=productdb
DB_USER=productuser
DB_PASSWORD=productpassword
