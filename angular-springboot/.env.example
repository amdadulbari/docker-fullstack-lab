# Backend
PORT=8080
DB_HOST=db
DB_PORT=5432
DB_NAME=contactsdb
DB_USER=contactuser
DB_PASSWORD=contactpassword
FRONTEND_URL=http://localhost:4200
JPA_SHOW_SQL=false

# Frontend reads from proxy.conf.json in Docker, direct URL for local dev
