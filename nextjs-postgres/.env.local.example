# PostgreSQL connection settings
# Used for LOCAL development (without Docker)
# Copy to .env.local: cp .env.local.example .env.local
#
# Note: DB_HOST=localhost because PostgreSQL runs directly on your machine,
# not inside a Docker network where it would be reachable as 'db'.
DB_HOST=localhost
DB_PORT=5432
DB_NAME=blogdb
DB_USER=bloguser
DB_PASSWORD=blogpassword
